import React from 'react';

const StationsPage: React.FC = () => {
    return (
        <div>
            Container for what goes into the stations page
        </div>
    );
};

export default StationsPage;